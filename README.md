# Blue Instinct TS Bot

## Prerequisites

- Node 14
- Docker
- Docker-Compose

## Starting The Application

1. copy `.env.sample` -> `.env` and fill out values
2. run `npm install`
3. run `docker-compose up -d`
